[package]
name = "audio-tempo"
version = "0.1.0"
edition = "2021"
description = "signalsmith-stretch 封装，为 C 音频引擎提供变速变调能力"
publish = false

[workspace]

[lib]
crate-type = ["staticlib"]

[dependencies]
signalsmith-stretch = "0.1.3"

[profile.release]
lto = true
strip = "symbols"
opt-level = 2
