# ── 端口 ──
SPLAYER_PORT=8080

# ── 时区 ──
TZ=Asia/Shanghai

# ── 内存限制 ──
MEMORY_LIMIT=512M

# ── 加密密钥（生产环境请更换为随机字符串）──
# PROXY_KEY=dev-proxy-key
# SPLAYER_ENCRYPTION_KEY=

# ── 构建参数 ──
# BUILD_JOBS=1
# CN_MIRROR=0
# SPLAYER_ARCH=amd64
